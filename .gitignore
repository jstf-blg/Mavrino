# Secrets — NEVER commit these
.env
config/wp_token.json
config/*token*.json
credentials.json
config/gsc_token.json

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/

# Product cache (can be large, regenerated automatically)
config/product_cache/

# Logs (optional — comment out if you want logs in repo)
logs/

# OS
.DS_Store
Thumbs.db
